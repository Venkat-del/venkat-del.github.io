var connect = require('connect');
var serveStatic = require('serve-static');
connect().use(serveStatic('C:\\Users\\vegangavarapu\\Documents\\Venkat\\Projects\\shift_index\\d3_project\\')).listen(80);
